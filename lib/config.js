const runDir = __dirname + "/../run/"
const runConfigDir = runDir + "config/"
const runLogDir = runDir + "logs/"
const playbookDir = __dirname + "/../playbooks/"
const playbookVarDir = runDir + "vars/"

module.exports = {
    runDir: runDir,
    runConfigDir: runConfigDir,
    runLogDir: runLogDir,
    playbookDir: playbookDir,
    playbookVarDir: playbookVarDir
}